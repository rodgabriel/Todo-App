import { Navbar } from "./styles";

import fireDb from "../../firebase";

export default function Index() {
    return (
        <Navbar>
            <div>LOGO</div>
            <div>LOGIN</div>
        </Navbar>
    );
}
